[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.mypy]
# strict = true
check_untyped_defs = true
exclude = ['src/casalib/scaffolding/templates']

[tool.pylint]
disable = [
    'duplicate-code',
    'import-outside-toplevel',
]